import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({ 
    screen: {
        marginTop: 75,
        marginBottom: 20,
        marginHorizontal: 30,
    }
})
  
export default styles;